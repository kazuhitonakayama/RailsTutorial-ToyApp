<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>
<% @microposts.each do |micropost| %>
  <%= micropost.content %>
<% end %>
<%# @micropost.content　で1個目のコメントの内容を見れる %>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
